import smtplib

def send_alert(message):
    print("Sending alert:", message)
    # You would configure actual email credentials here
    # smtp = smtplib.SMTP('smtp.example.com', 587)
    # smtp.login('user', 'password')
    # smtp.sendmail(from_addr, to_addr, message)
